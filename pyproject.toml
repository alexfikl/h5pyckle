[build-system]
requires = [
    "setuptools>=42",
    "wheel"
    ]
build-backend = "setuptools.build_meta"

[tool.black]
line-length = 85
target-version = ["py38"]

[tool.pyright]
pythonVersion = "3.8"
